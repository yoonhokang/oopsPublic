# 📘 oopsPublic 프로젝트 학습 가이드

> **대상 독자**: HTML, CSS, JavaScript, 서버리스 서비스를 처음 접하는 분
> **학습 방식**: 이 문서와 실제 소스 코드를 함께 보면서 진행합니다

---

## 목차

1. [프로젝트 소개](#1-프로젝트-소개)
2. [프로젝트 구조 맵](#2-프로젝트-구조-맵)
3. [핵심 개념 학습](#3-핵심-개념-학습)
4. [코드 따라가기 — 메인 페이지](#4-코드-따라가기--메인-페이지)
5. [코드 따라가기 — 비밀번호 생성기](#5-코드-따라가기--비밀번호-생성기)
6. [코드 따라가기 — 웹 페이지 클리퍼](#6-코드-따라가기--웹-페이지-클리퍼)
7. [보안 개념 정리](#7-보안-개념-정리)
8. [자주 묻는 질문 (FAQ)](#8-자주-묻는-질문-faq)

---

## 1. 프로젝트 소개

### 이 프로젝트는 무엇인가요?

**oopsPublic**은 두 가지 웹 도구를 제공하는 개인 프로젝트입니다:

| 도구 | 설명 |
|---|---|
| 🔒 **비밀번호 생성기** | Google 뉴스 헤드라인에서 단어를 추출하여 기억하기 쉬운 비밀번호 생성 |
| 📧 **웹 페이지 클리퍼** | URL을 입력하면 해당 페이지의 콘텐츠를 가져와 이메일로 전송하거나 데이터베이스에 저장 |

### 사용된 기술 스택

```
┌─────────────────────────────────────────┐
│  프론트엔드 (브라우저에서 실행)          │
│  ┌──────────┐ ┌─────┐ ┌────────────┐   │
│  │  HTML     │ │ CSS │ │ JavaScript │   │
│  │ (구조)   │ │(디자인)│ │ (동작)    │   │
│  └──────────┘ └─────┘ └────────────┘   │
├─────────────────────────────────────────┤
│  백엔드 (서버리스 — Google Firebase)     │
│  ┌──────────────┐ ┌─────────────────┐  │
│  │ Firebase Auth │ │ Firestore DB    │  │
│  │ (Google 로그인)│ │ (데이터 저장)  │  │
│  └──────────────┘ └─────────────────┘  │
│            ↕               ↕            │
│  ┌──────────────────────────────────┐  │
│  │ Firestore Rules (보안 규칙)      │  │
│  │ "누가 무엇을 할 수 있는지" 정의  │  │
│  └──────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

> **서버리스(Serverless)란?** 서버 컴퓨터를 직접 관리하지 않고, Google(Firebase)이 제공하는 서비스를 API로 호출하여 백엔드 기능을 사용하는 방식입니다. 서버 장비 구매, 운영체제 설치, 보안 패치 등을 신경 쓸 필요가 없습니다.

---

## 2. 프로젝트 구조 맵

```
oopsPublic/
├── index.html              ← 🏠 메인 페이지 (도구 목록을 카드로 표시)
├── css/
│   └── style.css           ← 🎨 전체 공통 스타일 (색상, 레이아웃, 컴포넌트)
├── js/
│   ├── api-config.js       ← ⚙️ Firebase 설정값 + API 끝점(Endpoint) 정의
│   ├── auth.js             ← 🔐 Google 로그인/로그아웃 + 인증 UI 렌더링
│   ├── debug-monitor.js    ← 🐛 에러 자동 감지 + 로깅 시스템 (window.Logger)
│   └── main.js             ← 📄 메인 페이지 전용 로직 (인증 가드, 토스트 메시지)
├── generatePassWd/
│   ├── index.html          ← 🔒 비밀번호 생성기 페이지
│   ├── script.js           ← 비밀번호 생성 핵심 로직
│   └── style.css           ← 비밀번호 생성기 전용 스타일
├── webPageByEmail/
│   ├── index.html          ← 📧 웹 클리퍼 페이지
│   ├── script.js           ← 웹 클리핑/이메일 전송/게시판 로직
│   └── style.css           ← 웹 클리퍼 전용 스타일
├── firestore.rules         ← 🛡️ Firestore 데이터베이스 보안 규칙
└── run_server.bat          ← ▶️ 로컬 테스트 서버 실행용 배치 파일
```

### 파일 로드 순서 (매우 중요!)

HTML 파일에서 `<script>` 태그의 순서가 바뀌면 동작하지 않습니다:

```
① api-config.js    → window.API_CONFIG 정의
② debug-monitor.js → window.Logger 정의 (①의 debugMode 참조)
③ auth.js          → Firebase 초기화 (①의 firebaseConfig 참조)
④ main.js 또는 script.js → 페이지별 비즈니스 로직 (①②③ 모두 참조)
```

---

## 3. 핵심 개념 학습

### 3.1 HTML — 구조를 만드는 언어

HTML(HyperText Markup Language)은 웹 페이지의 **뼈대**를 만드는 언어입니다.

```html
<!-- 예시: index.html의 카드 구조 -->
<a href="./generatePassWd/index.html" class="card">   ← 링크 (클릭하면 이동)
    <div>                                                ← 콘텐츠 감싸기
        <h2>Secure Password Generator</h2>               ← 제목
        <p>Generate strong, memorable passwords...</p>   ← 설명 문장
    </div>
    <div class="card-footer">                            ← 하단 영역
        <span>Updated: 2026-02-16</span>                 ← 날짜 텍스트
        <span class="arrow">Launch →</span>              ← 화살표
    </div>
</a>
```

**핵심 태그 요약:**

| 태그 | 역할 | 이 프로젝트에서의 사용 |
|---|---|---|
| `<div>` | 영역 나누기 (의미 없는 그릇) | 레이아웃 구성 |
| `<a>` | 링크 (다른 페이지로 이동) | 카드 전체를 클릭 가능하게 |
| `<button>` | 클릭 가능한 버튼 | Generate, Save, Delete |
| `<input>` | 사용자 입력 필드 | URL 입력, 비밀번호 길이 |
| `<script>` | JS 파일 로드 | 각 페이지 하단에 위치 |
| `<link>` | CSS 파일 연결 | `<head>` 안에 위치 |
| `<meta>` | 페이지 메타데이터 | 문자셋, CSP 보안 정책 |

### 3.2 CSS — 디자인을 입히는 언어

CSS(Cascading Style Sheets)는 HTML 요소의 **모양과 배치**를 결정합니다.

```css
/* css/style.css에서 발췌 */

/* CSS 변수 정의 — 한 곳에서 색상을 관리 */
:root {
    --bg-color: #0f172a;       /* 배경색: 짙은 남색 */
    --accent-color: #38bdf8;   /* 강조색: 하늘색 */
}

/* body 스타일 — 페이지 전체에 적용 */
body {
    background-color: var(--bg-color);  /* 변수 참조 */
    color: var(--text-color);
    display: flex;                       /* Flexbox 레이아웃 사용 */
    flex-direction: column;              /* 세로 방향 배치 */
    align-items: center;                 /* 가로 중앙 정렬 */
}

/* 카드 호버 효과 — 마우스를 올리면 위로 떠오르는 느낌 */
.card:hover {
    transform: translateY(-5px);         /* 위로 5px 이동 */
    box-shadow: 0 10px 20px rgba(0,0,0,0.3);  /* 그림자 추가 */
}
```

**CSS 핵심 개념:**

| 개념 | 설명 | 파일에서 찾기 |
|---|---|---|
| **CSS 변수** (`--변수명`) | 색상 등을 한 곳에서 관리 | `css/style.css` `:root` |
| **Flexbox** | 요소들을 한 줄로 정렬 | `display: flex` 검색 |
| **Grid** | 격자형 레이아웃 | `css/style.css` `.landing-container` |
| **Transition** | 부드러운 변화 효과 | `transition: transform 0.2s` |
| **@media** | 화면 크기별 분기 | 파일 하단 `@media (max-width: 600px)` |
| **backdrop-filter** | 유리 흐림 효과 | `blur(10px)` 검색 |

### 3.3 JavaScript — 동작을 부여하는 언어

JavaScript는 웹 페이지에 **사용자 상호작용과 로직**을 추가합니다.

#### IIFE (즉시 실행 함수 표현식)

이 프로젝트의 모든 JS 파일은 아래 패턴으로 감싸져 있습니다:

```javascript
(function () {
    "use strict";    // 엄격 모드 — 실수를 에러로 잡아줌

    // 이 안의 변수와 함수는 "밖"에서 접근 불가
    let mySecret = "비밀";

    // 꼭 필요한 것만 밖으로 내보냄
    window.myPublicFunction = function() { ... };

})();  // ← 정의 즉시 실행!
```

**왜 쓰는가?** 여러 JS 파일이 로드될 때, 같은 이름의 변수가 충돌하는 것(전역 오염)을 방지합니다.

#### 주요 JavaScript 패턴

| 패턴 | 코드에서 찾기 | 설명 |
|---|---|---|
| **`addEventListener`** | `버튼.addEventListener('click', 함수)` | HTML의 onclick= 대신 JS에서 이벤트 연결 |
| **`DOMContentLoaded`** | `document.addEventListener('DOMContentLoaded', ...)` | HTML 파싱 완료 후 실행 |
| **`async/await`** | `async function getToken() { await ... }` | 비동기 작업을 순차적으로 표현 |
| **`fetch`** | `await fetch(url, options)` | 서버에 HTTP 요청 전송 |
| **`createElement`** | `document.createElement('div')` | JS로 HTML 요소 동적 생성 |

### 3.4 서버리스 (Firebase)

#### Firebase Authentication (인증)

사용자가 **Google 계정으로 로그인**하는 기능입니다.

```
사용자 → "Sign in with Google" 클릭
         ↓
       Google 로그인 팝업 표시 (Firebase SDK 처리)
         ↓
       로그인 성공 → Firebase가 "이 사용자입니다" 확인
         ↓
       onAuthStateChanged 콜백 → UI 업데이트 (프로필 표시)
         ↓
       getIdToken() → "출입증(토큰)" 발급
         ↓
       REST API 요청 시 토큰 첨부 → Firestore가 "이 사용자의 데이터만" 허용
```

> **📂 관련 코드**: `js/auth.js`에서 `loginWithGoogle()`, `getAuthIdToken()`, `registerAuthListener()` 함수를 확인하세요.

#### Firestore REST API (데이터베이스)

데이터를 읽고 쓰는 기능입니다. 이 프로젝트에서는 SDK 대신 **REST API**(HTTP 요청)를 사용합니다.

```
                  fetch() 함수로 HTTP 요청
                  ┌────────────────────┐
                  │                    ↓
    브라우저       │        Firestore REST API
    (script.js)   │        (Google 서버)
                  │                    │
                  │    JSON 응답       │
                  │ ←──────────────── │
                  └────────────────────┘
```

**CRUD 동작 예시:**

| 동작 | HTTP 메서드 | 코드 위치 |
|---|---|---|
| **읽기** (게시물 목록) | POST (runQuery) | `webPageByEmail/script.js` → `loadBoardREST()` |
| **쓰기** (게시물 저장) | POST | `webPageByEmail/script.js` → `saveToBoard()` |
| **삭제** (게시물 삭제) | DELETE | `webPageByEmail/script.js` → `deletePost()` |

> **💡 REST API란?** URL에 HTTP 요청(GET, POST, PUT, DELETE)을 보내 데이터를 주고받는 표준 방식입니다. 웹 브라우저의 `fetch()` 함수로 실행합니다.

#### Firestore Rules (보안 규칙)

누가 어떤 데이터에 접근할 수 있는지를 정의하는 규칙입니다.

```
// firestore.rules에서 발췌
match /users/{userId} {
    // 본인(request.auth.uid)과 문서 ID(userId)가 같아야만 허용
    allow read, write: if request.auth.uid == userId;
}
```

**핵심 원리**: 모든 접근은 기본적으로 **거부(deny)**되며, 규칙에 맞는 경우에만 **허용(allow)**합니다.

---

## 4. 코드 따라가기 — 메인 페이지

> **파일**: `index.html`, `js/main.js`, `css/style.css`

### 페이지 로드 시 일어나는 일

```
[1] 브라우저가 index.html을 파싱
     ↓
[2] <link href="css/style.css"> → 스타일시트 로드
     ↓
[3] <script src="js/api-config.js"> → Firebase 설정값 정의
     ↓
[4] <script src="js/debug-monitor.js"> → window.Logger 등록
     ↓
[5] <script src="js/auth.js"> → Firebase 초기화 + 로그인 UI 준비
     ↓
[6] HTML 본문(<body>) 렌더링
     ↓
[7] <script src="js/main.js"> → DOMContentLoaded 대기
     ↓
[8] HTML 파싱 완료 → DOMContentLoaded 이벤트 발생
     ↓
[9] main.js: 인증 리스너 등록 + 카드 클릭 가드 설정
     ↓
[10] auth.js: onAuthStateChanged → 로그인 버튼 또는 프로필 표시
```

### 인증 가드의 동작 원리

```javascript
// js/main.js에서 발췌
cards.forEach(card => {
    card.addEventListener('click', (e) => {
        const user = firebase.auth().currentUser;  // 현재 로그인 사용자 확인

        if (!user) {
            e.preventDefault();  // 기본 동작(페이지 이동) 차단!
            showToast("🔒 로그인이 필요합니다.");  // 토스트 메시지 표시
        }
        // user가 있으면 → 아무것도 하지 않음 → 기본 동작(링크 이동) 실행
    });
});
```

---

## 5. 코드 따라가기 — 비밀번호 생성기

> **파일**: `generatePassWd/index.html`, `generatePassWd/script.js`

### 비밀번호 생성 과정

```
[1] 사용자가 "Generate Secure Password" 버튼 클릭
     ↓
[2] generatePassword() 실행
     ↓
[3] fetchNewsSentence(길이+5)
     │  ├─ Google News RSS → rss2json.com API로 가져오기
     │  ├─ 실패하면 FALLBACK_WORDS 사용
     │  └─ 결과: ["Apple", "Brave", "Cloud", ...] + "!" (특수문자)
     ↓
[4] extractPasswordCharacters(tokens, 8)
     │  ├─ 1회전: 모든 단어의 1번째 글자 수집 → A, B, C, ...
     │  ├─ 2회전: 모든 단어의 2번째 글자 수집 → p, r, l, ...
     │  └─ 마지막에 특수문자(!) 추가
     ↓
[5] 결과 → "ABCDprl!" (8자리 비밀번호)
     ↓
[6] 화면에 비밀번호 + 원본 문장(니모닉) 표시
```

### 보안 난수의 중요성

```javascript
// generatePassWd/script.js에서 발췌

// ❌ 나쁜 예: Math.random()은 예측 가능
function insecureRandom(max) {
    return Math.floor(Math.random() * max);  // 보안 부적합!
}

// ✅ 좋은 예: crypto.getRandomValues()는 운영체제 엔트로피 사용
function secureRandom(max) {
    const array = new Uint32Array(1);
    const limit = Math.floor(0x100000000 / max) * max;  // 바이어스 제거
    do {
        window.crypto.getRandomValues(array);
    } while (array[0] >= limit);
    return array[0] % max;
}
```

---

## 6. 코드 따라가기 — 웹 페이지 클리퍼

> **파일**: `webPageByEmail/index.html`, `webPageByEmail/script.js`

### CORS 프록시란?

브라우저의 보안 정책상, 다른 도메인의 HTML을 직접 `fetch()`로 가져올 수 없습니다.
이를 **CORS(Cross-Origin Resource Sharing)** 정책이라고 합니다.

```
[브라우저] ──fetch──→ [example.com]     ← ❌ CORS 차단!

[브라우저] ──fetch──→ [corsproxy.io] ──→ [example.com]  ← ✅ 프록시 경유!
```

```javascript
// webPageByEmail/script.js에서 발췌
const proxies = [
    { name: 'CorsProxy.io', url: `https://corsproxy.io/?${encodeURIComponent(targetUrl)}` },
    { name: 'AllOrigins',   url: `https://api.allorigins.win/get?url=${...}` },
    { name: 'CodeTabs',     url: `https://api.codetabs.com/v1/proxy?quest=${...}` }
];
// 첫 번째 프록시가 실패하면 두 번째, 세 번째 순으로 시도 (폴백 패턴)
```

### REST API로 게시물 저장하기

```javascript
// webPageByEmail/script.js → saveToBoard() 핵심 부분

const token = await window.getAuthIdToken();  // 인증 토큰 획득

const endpoint = `${API_CONFIG.endpoints.firestore}/users/${user.uid}/web_clipper`;
//                  ↑ Firestore REST API 주소            ↑ 사용자별 경로

await fetch(endpoint, {
    method: "POST",                                    // 새 문서 생성
    headers: {
        "Content-Type": "application/json",            // 보내는 데이터 형식
        "Authorization": `Bearer ${token}`             // 인증 토큰 첨부
    },
    body: JSON.stringify({                             // 저장할 데이터
        fields: {
            title: { stringValue: "페이지 제목" },     // Firestore 필드 형식
            url: { stringValue: "https://..." },
            content: { stringValue: "<html>..." },
            createdAt: { timestampValue: "2026-..." }
        }
    })
});
```

### HTML 정제 (Sanitization)

데이터베이스에서 꺼낸 HTML을 그대로 화면에 표시하면 **XSS 공격**에 노출됩니다.

```javascript
// webPageByEmail/script.js → sanitizeHtml() 핵심 부분

function sanitizeHtml(htmlString) {
    const parser = new DOMParser();
    const doc = parser.parseFromString(htmlString, 'text/html');

    // 1. 위험 태그 제거: script, iframe, object 등
    doc.querySelectorAll('script, iframe, object').forEach(el => el.remove());

    // 2. 위험 속성 제거: onclick, onerror 등
    doc.querySelectorAll('*').forEach(el => {
        for (const attr of el.attributes) {
            if (attr.name.startsWith('on')) el.removeAttribute(attr.name);
        }
    });

    return doc.body.innerHTML;  // 정제된 안전한 HTML 반환
}
```

---

## 7. 보안 개념 정리

### CSP (Content Security Policy)

HTTP 헤더 또는 `<meta>` 태그로 설정하는 **리소스 출처 화이트리스트**입니다.

```html
<!-- index.html에서 발췌 -->
<meta http-equiv="Content-Security-Policy"
    content="
        default-src 'self';                    ← 기본: 같은 출처만
        script-src 'self' https://*.gstatic.com;  ← JS: 자신 + Google
        connect-src 'self' https://firestore.googleapis.com;  ← API 호출 대상
    ">
```

| 지시어(Directive) | 제어 대상 |
|---|---|
| `default-src` | 다른 지시어가 없으면 이 규칙 적용 |
| `script-src` | JavaScript 파일 출처 |
| `style-src` | CSS 파일 출처 |
| `connect-src` | fetch/XHR 요청 대상 |
| `img-src` | 이미지 출처 |
| `frame-src` | iframe 삽입 허용 출처 |

### XSS (Cross-Site Scripting)

공격자가 웹 페이지에 **악성 스크립트를 삽입**하는 공격입니다.

```
❌ 위험: innerHTML에 사용자 데이터를 직접 삽입
   element.innerHTML = user.displayName;
   → 만약 displayName이 "<img onerror=alert('해킹!')>"이면 실행됨!

✅ 안전: textContent 사용 (HTML 태그를 텍스트로 처리)
   element.textContent = user.displayName;
   → "<img onerror=...>" 가 그대로 "텍스트"로 표시됨
```

> **📂 관련 코드**: `js/auth.js`의 `renderAuthUI()` 함수에서 `textContent` 사용, `webPageByEmail/script.js`의 `sanitizeHtml()` 함수

### IIFE (즉시 실행 함수 표현식)

```javascript
// 전역 오염 방지를 위한 패턴
(function () {
    // 이 안의 변수는 밖에서 접근 불가
    const secret = "비밀";
})();

console.log(secret);  // ❌ ReferenceError: secret is not defined
```

---

## 8. 자주 묻는 질문 (FAQ)

### Q. Firebase API 키가 코드에 노출되어 있는데 괜찮나요?

**네, 괜찮습니다.** Firebase API 키는 "비밀번호"가 아니라 서비스를 식별하는 "주소"에 가깝습니다. 실제 보안은 Firestore Rules와 인증 토큰이 담당합니다. 키가 노출되어도 규칙에 의해 다른 사용자의 데이터에 접근할 수 없습니다.

### Q. 왜 Firestore SDK를 직접 쓰지 않고 REST API를 사용하나요?

이 프로젝트의 Firestore는 **"Datastore 모드"**로 설정되어 있어, 웹 SDK(firebase-firestore.js)로 직접 접근이 불가능합니다. 대신 `fetch()` 함수로 REST API에 HTTP 요청을 보내 같은 기능을 수행합니다.

### Q. CORS 프록시를 왜 3개나 사용하나요?

무료 CORS 프록시는 트래픽 제한, 일시적 장애 등으로 언제든 실패할 수 있습니다. 첫 번째 프록시가 실패하면 두 번째를 시도하고, 그것도 실패하면 세 번째를 시도하는 **폴백(Fallback) 패턴**으로 안정성을 높였습니다.

### Q. `"use strict";`는 무엇인가요?

JavaScript의 **엄격 모드**를 활성화합니다. 변수 선언 없이 사용(`x = 10;`)하거나, 읽기 전용 속성에 값을 할당하는 등의 실수를 에러로 잡아줍니다. 모든 현대 JS 프로젝트에서 권장됩니다.

### Q. `DOMContentLoaded`와 `window.onload`는 무엇이 다른가요?

| 이벤트 | 발생 시점 |
|---|---|
| `DOMContentLoaded` | HTML 파싱이 완료된 직후 (이미지, CSS 로드 무관) |
| `window.onload` | 모든 리소스(이미지, CSS 등)가 완전히 로드된 후 |

이 프로젝트에서는 `DOMContentLoaded`를 사용하여 빠르게 초기화합니다.

---

> 📌 **학습 팁**: 각 파일의 최상단 주석(파일 헤더)을 먼저 읽으면 파일의 역할과 데이터 흐름을 빠르게 파악할 수 있습니다. 코드에 `【이 파일의 역할】`, `【핵심 함수 흐름】` 등의 구조화된 주석이 포함되어 있으니 참고하세요.
